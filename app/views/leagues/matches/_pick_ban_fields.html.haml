%li.list-group-item.nested-fields
  .form-inline
    .col-sm-5= f.select :kind, f.object.class.kinds.map { |key, _| [key.humanize, key] }
    .col-sm-5= f.select :team, f.object.class.teams.map { |key, _| [key.humanize, key] }

  = link_to_remove_association f, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-trash
    Delete
