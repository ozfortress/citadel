- user = present(user)

.row.slim-gutter
  .pull-left
    = user.avatar_link

  .user-details
    .row.slim-gutter
      = user.link
      = user.titles team: local_assigns[:team]
      .steam-link= user.steam_link
