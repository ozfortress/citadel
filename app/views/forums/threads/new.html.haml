%h4.pull-right Create a new Thread

= render 'forums/head', object: @thread.topic

= bootstrap_form_for @thread do |f|
  = render 'form', f: f

  = f.submit 'Create'
  = link_to 'Cancel', :back, class: 'btn btn-default'
