- if object
  - presenter = present(object)

  %nav.my-3
    %ol.breadcrumb.bg-transparent.pl-0.py-1.mb-0
      %li.breadcrumb-item
        = link_to forums_path(nil) do
          Home

      = presenter.breadcrumbs

    %h3
      = presenter

- else
  %nav.my-3
    %ol.breadcrumb.bg-transparent.pl-0.py-1.mb-0
      %li.breadcrumb-item.active
        Home

    %h3
      Forums
