%h4.pull-right Create a new Topic

= render 'forums/head', object: @topic.parent_topic

= bootstrap_form_for @topic do |f|
  = render 'form', f: f

  = f.submit 'Create'
  = link_to 'Cancel', :back, class: 'btn btn-default'
