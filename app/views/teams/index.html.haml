%h1 Teams

.row
  .col-sm-3.col-md-3.pull-right
    = render 'search'

.panel.panel-default
  - if user_signed_in?
    .panel-body
      = link_to new_team_path, class: 'btn btn-default pull-right', type: 'button' do
        Start Team
        %span.glyphicon.glyphicon-plus

  - if @teams.size > 0
    %ul.list-group
      - @teams.each do |team|
        %li.list-group-item
          = link_to team_path(team) do
            = team.name
  - else
    .panel-body No teams found...

= will_paginate @teams, renderer: BootstrapPagination::Rails
