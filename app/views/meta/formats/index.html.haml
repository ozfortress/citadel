= link_to new_meta_format_path, class: 'btn btn-secondary pull-right', type: 'button' do
  Add Format
  = inline_svg_tag 'open_iconic/plus.svg', class: 'icon ml-1'

%h1 Formats

%ul.list-group.mb-3
  - @formats.each do |format|
    - format_p = present(format)
    %li.list-group-item
      = format_p.link

      = link_to edit_meta_format_path(format), class: 'btn btn-secondary btn-sm pull-right' do
        Edit
        = inline_svg_tag 'open_iconic/pencil.svg', class: 'icon ml-1'
