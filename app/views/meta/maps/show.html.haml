- if user_can_meta?
  = link_to edit_meta_map_path(@map), class: 'btn btn-secondary pull-right', type: 'button' do
    Edit
    = inline_svg_tag 'open_iconic/pencil.svg', class: 'icon ml-1'

- map_p = present(@map)

%h1= map_p.game.link
%h2= @map.name

.markdown.mb-3= map_p.description
