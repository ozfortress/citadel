= bootstrap_form_for @map,
                     url: { controller: 'meta/maps', action: 'create' } do |f|
  = render 'map_form', f: f

  .d-flex.justify-content-end
    = link_to 'Cancel', meta_maps_path, class: 'btn btn-link mr-2'
    = f.submit 'Create'
