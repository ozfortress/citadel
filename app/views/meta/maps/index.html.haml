= link_to new_meta_map_path, class: 'btn btn-secondary pull-right', type: 'button' do
  Add Map
  %span.glyphicon.glyphicon-plus

%h1 Maps

%ul.list-group
  - @maps.each do |map|
    - map_p = present(map)
    %li.list-group-item
      = map_p.link

      .button-group.pull-right
        = link_to edit_meta_map_path(map), class: 'btn btn-secondary' do
          Edit
          %span.glyphicon.glyphicon-cog
